When the source exceeds four minutes, select one self-contained passage with a strong opening and clean landing. Use transcript word IDs and timestamps only; never invent or rewrite quotes. Return strict JSON only.
