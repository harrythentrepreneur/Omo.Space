# Prompt — open-source-model × hub-spoke × omo-terminal

Output: one flat-vector social graphic, 16:9 landscape (1600x900), text inside
central 84% safe area.

Viewer takeaway: "Omo's open-source model is real: free skills, MIT, pay only
for the run."

Layout (hub-spoke): a large central rounded pine panel holding the hub label
`OPEN SOURCE` with sub-line `omo.space/skills`; six evenly spaced spokes radiate
from it like terminal log lines, each a short cream/mint pill with one exact
label: `MIT`, `15 free skills`, `FREE to download`, `Pay per run`, `Readable`,
`1 flagship paid`. Spokes are independent, no cross-links.

Theme (omo-terminal — full spec in references/styles/omo-terminal.md):
- Canvas: cream `#F8F7F5`; one terminal-window panel with a pine `#17352C`
  title bar (`omo@space:~$` in cream, three small dots: orange `#FF6B3D`,
  peach `#FFB89D`, mint `#BDEFD4`) and a pale-mint `#EAF7F0` field.
- Headline is an ASCII-art banner rendered as real monospace characters inside
  a plain box-drawing stone border (`+---+` / `|   |`): the string
  `$ omo model --open` in pine; that line is the ONE orange element.
- All body copy monospace (Menlo/Monaco visual direction): pine `#17352C`
  text, muted `#5F6F68` captions, quiet rule `#D9E2DC`.
- Flat vector only: solid fields, crisp edges, rounded panel corners, no
  gradients, no 3D, no gloss, no fake editor windows, no neon, no glitch.

Exact on-image strings (spelling is mandatory): `$ omo model --open`,
`OPEN SOURCE`, `omo.space/skills`, `MIT`, `15 free skills`,
`FREE to download`, `Pay per run`, `Readable`, `1 flagship paid`, `omo.space`.

Reading order: headline banner → hub → spokes (top-left to bottom-right) →
CTA pill `omo.space` at the bottom edge.

End with: one finished social graphic, no device mockup, no photo, no 3D
render, no watermark, no fake UI, no extra words, no misspelled text.
