# gpt-image-2 prompt — skill-md-loader (issue #115, wave 2)

One finished 16:9 landscape flat-vector social graphic, 1600x900 canvas.

Viewer takeaway: give Omo your skill.md and you get a built, tested, priced
workflow — one flat $5 build, not a subscription.

Layout geometry — Omo master style, linear-progression variant:
- Cream canvas #F8F7F5 filling ~45% of pixels as open breathing room.
- A single rounded-rect pine #17352C frame, 9px stroke, radius 28, inset 24px
  from the canvas edge. No double frame, no shadow, no white frame.
- Eyebrow top-left, uppercase DM Sans 700 letterspaced 20px, muted #5F6F68.
- Fraunces 600 headline top-left block, 2 lines, 64-80px, pine #17352C on
  cream (never on a field). Headline occupies columns 1-8.
- Three numbered nodes along one horizontal path across the lower two-thirds
  (columns 2-11), each a white #FFFFFF rounded tile with one flat icon and
  one short DM Sans 700 pine label beneath; a thin pine connector line
  #D9E2DC joins the tiles, with a small flat arrow between tiles.
- Price tag on the third tile: the ONE orange element.
- CTA pill bottom-left: cream #FFFFFF pill, pine DM Sans 700 text.

Theme: omo-mint-flat — crisp utility, scannable. One pale mint #EAF7F0 field
blob behind the node row, one mint #BDEFD4 rounded blob behind the headline
area. Deep mint #78CCA1 allowed for the connector line only. Flat vector
only: solid fills, mono-weight pine outlines with round caps, one oversized
central icon per tile. No gradients, no gloss, no 3D, no photo, no texture.

Illustration subjects, one per tile, flat vector:
- Tile 1: a simple document sheet icon (folded corner) — the skill.md file.
- Tile 2: a checklist card with two small pine checks.
- Tile 3: a price tag on a string, cream with pine outline — the priced
  result. No bean needed; composition stays perfectly level.

Exact on-image strings (spelling must be exact):
- Eyebrow: `OMO · SKILL LOADER`
- Headline, two lines: `Your skill.md` / `built, tested, priced.`
- Tile labels, three: `1 skill.md` / `2 built & tested` / `3 priced`
- Price tag text (the ONE orange #FF6B3D element): `$5`
- CTA pill: `omo.space`

Reading order: eyebrow, headline, node 1, node 2, node 3, CTA. Total words
<= 16.

Safe area: keep all text inside the central 84% of the canvas; the pine frame
stays clear of text.

Negative constraints: no device mockup, no laptop, no terminal window, no
code editor, no photo, no 3D render, no gradients, no watermark, no fake UI,
no other logos, no second orange element (only the $5 price tag), no black or
gray ink, no tilted elements, no confetti. One finished social graphic, no
device mockup, no photo, no 3D render, no watermark, no fake UI, no extra
words, no misspelled text.
