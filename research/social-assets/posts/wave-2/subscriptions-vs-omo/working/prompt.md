# gpt-image-2 prompt — subscriptions-vs-omo (issue #115, wave 2)

One finished 16:9 landscape flat-vector social graphic, 1600x900 canvas.

Viewer takeaway: subscriptions bill you every month; Omo charges once per
run — an honest, calm two-panel comparison with no competitor names.

Layout geometry — Omo master style, binary-comparison variant:
- Cream canvas #F8F7F5 filling ~50% of pixels as open breathing room.
- A single rounded-rect pine #17352C frame, 9px stroke, radius 28, inset 24px
  from the canvas edge. No double frame, no shadow, no white frame.
- Eyebrow top-left, uppercase DM Sans 700 letterspaced 20px, muted #5F6F68.
- Fraunces 600 headline centered top block, 2 lines, 56-68px, pine #17352C
  on cream (never on a field).
- Two balanced white #FFFFFF rounded panels side by side (columns 1-6 and
  7-12), split by one clean vertical center seam: a thin pine rule #D9E2DC.
  Mirror the visual anchors on each side.
- CTA pill bottom-center: cream #FFFFFF pill, pine DM Sans 700 text.

Theme: omo-sweet-pastel — warm, honest kitchen-table tone. Two pastel fields
max: one muted mint #EAF7F0 tint behind the left panel and one soft peach
#FFB89D tint behind the right panel (fields sit behind the white panels,
never behind text). Flat vector only: solid fills, smooth pine outlines with
round caps, soft pine-tinted shadow only. No gradients, no gloss, no 3D, no
photo, no texture.

Illustration subjects, one per panel, flat vector:
- Left panel (Subscriptions): a small flat calendar page with three small
  repeating pine `$` chips in a row — the recurring bill. All pine and muted
  mint; no orange anywhere on this side.
- Right panel (Omo): one flat result card with a single orange #FF6B3D check
  mark — the one orange accent in the whole image. Everything else pine.

Exact on-image strings (spelling must be exact):
- Eyebrow: `OMO · PAY PER USE`
- Headline, two lines: `Subscriptions vs Omo:` / `pay per use.`
- Left panel label and line: `SUBSCRIPTIONS` and `billed every month`
- Right panel label and line: `OMO` and `pay per run, keep it`
- CTA pill: `omo.space`

Reading order: eyebrow, headline, left panel, right panel, CTA. Total words
<= 16.

Safe area: keep all text inside the central 84% of the canvas; the pine frame
stays clear of text.

Negative constraints: no competitor logos, no brand names, no device mockup,
no photo, no 3D render, no gradients, no watermark, no fake UI, no other
logos, no second orange element (only the one check), no black or gray ink,
no tilted elements, no confetti. One finished social graphic, no device
mockup, no photo, no 3D render, no watermark, no fake UI, no extra words, no
misspelled text.
