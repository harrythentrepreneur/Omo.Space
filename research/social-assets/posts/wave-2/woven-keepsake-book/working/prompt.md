# gpt-image-2 prompt — woven-keepsake-book (issue #115, wave 2)

One finished 16:9 landscape flat-vector social graphic, 1600x900 canvas.

Viewer takeaway: Woven turns a WhatsApp chat export into a keepsake book you
keep — a warm, kitchen-table gift, priced per run.

Layout geometry — Omo master style, product-card variant:
- Cream canvas #F8F7F5 filling ~50% of pixels as open breathing room.
- A single rounded-rect pine #17352C frame, 9px stroke, radius 28, inset 24px
  from the canvas edge. No double frame, no shadow, no white frame.
- Eyebrow top-left, uppercase DM Sans 700 letterspaced 20px, muted #5F6F68.
- Fraunces 600 headline bottom-left block, 3 lines, 64-76px, pine #17352C on
  cream (never on a field). Headline occupies columns 1-7.
- Hero object right-center (columns 8-12): one small hardcover keepsake book.
- Price chip top-right of the hero object: the ONE orange element.
- Two fact chips in a row beneath the headline, white #FFFFFF pills, pine
  DM Sans 700 text.
- CTA pill bottom-left: cream #FFFFFF pill, pine DM Sans 700 text.

Theme: omo-cream-editorial — quiet, premium keepsake feel. Two or three soft
fields only: one muted mint #DCEFE4 rounded organic blob and one soft peach
#F8D6C7 blob behind the hero book, never behind text. Thin cool rules
#D9E2DC. Sparse pine line art with one flat pastel fill per object. Flat
vector only: solid fills, smooth pine outlines with round caps, soft
pine-tinted shadow only. No gradients, no gloss, no 3D, no photo, no texture.

Illustration subject: one small hardcover keepsake book (pine outline, cream
pages, a thin warm butter #F4E6B7 spine), rising out of a gentle upward
stream of 3 rounded chat bubbles beneath it — the bubbles are empty: each a
flat pastel fill (muted mint, soft peach, warm butter) with three small pine
dots inside, no text, no letters, no real chat content. The book is the one
hero object; the bean mascot may peek from the frame's top-left corner,
tilted exactly -7 degrees, with a tiny pine `•ᴗ•` face. Nothing else
rotates.

Exact on-image strings (spelling must be exact):
- Eyebrow: `WOVEN · KEEPSAKE BOOK`
- Headline, three lines: `Your WhatsApp chat,` / `turned into a` /
  `keepsake book.`
- Price chip (the ONE orange #FF6B3D element): `$0.40 per run`
- Fact chips, two: `PDF keepsake` and `You keep it`
- CTA pill: `omo.space`

Reading order: eyebrow, headline, price chip, fact chips, CTA. Total words
<= 16.

Safe area: keep all text inside the central 84% of the canvas; the pine frame
stays clear of text.

Negative constraints: no device mockup, no phone, no laptop, no photo, no 3D
render, no gradients, no watermark, no fake UI, no other logos, no real chat
conversation text, no second orange element (only the one price chip), no
black or gray ink, nothing else tilted, no confetti, no sticker piles. One
finished social graphic, no device mockup, no photo, no 3D render, no
watermark, no fake UI, no extra words, no misspelled text.
