# Prompt (kept for provenance) — decodable-book-card

This post was rendered programmatically for exact brand control (exact hexes, real Fraunces/DM Sans, exact copy) via research/social-assets/style-sheet/render_variants.py + omo_style.py.

Composition spec: master style — product variant (hero object = storybook with 9px pine outlines, butter cat with dot eyes, price chip = the one orange accent, fact chips, CTA) per research/OMO-IMAGE-STYLE-SYSTEM.MD. On-image wording: DECODABLE BOOK MAKER / $0.99 (orange price chip) / 4-page phonics PDF / Keep it forever / No subscription / Make your book.
