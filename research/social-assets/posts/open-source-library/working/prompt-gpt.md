# gpt-image-2 prompt — open-source-library (issue #87)

One finished 16:9 landscape flat-vector social graphic, 1600x900 canvas.

Viewer takeaway: Omo's skills library is open source — every skill is free to
download, you pay only when you run it.

Layout geometry — Omo master style, launch variant with hero repo panel:
- Cream canvas #F8F7F5 filling ~50% of pixels as open breathing room.
- A single rounded-rect pine #17352C frame, 9px stroke, radius 28, inset 24px
  from the canvas edge. No double frame, no shadow, no white frame.
- Eyebrow top-left, uppercase DM Sans 700 letterspaced 20px, muted #5F6F68.
- Fraunces 600 headline bottom-left block, 2 lines, 72-88px, pine #17352C on
  cream (never on a field). Headline occupies columns 1-7.
- Hero panel bottom-right (columns 8-12): one terminal-window panel on a pale
  mint field.
- CTA line under the headline: `omo.space`.

Theme: omo-terminal — the open-source Omo. Cream paper #F8F7F5, one
terminal-window panel: pine #17352C title bar with three small cream dots,
pale mint #EAF7F0 body field, thin pine rules #D9E2DC, rounded corners,
monospace body text (Menlo/DejaVu Sans Mono style), flat vector only.
Strict monospace grid, generous margins. The `$` prompt is always pine.
One orange #FF6B3D highlight line max.

Illustration subject: one terminal-window panel, drawn flat with thin pine
rules. Body copy reads like a terminal transcript: a `$` prompt, one short
command line, one indented output line. No fake editor chrome, no syntax
highlighting colors.

Exact on-image strings (spelling must be exact):
- Eyebrow: `OMO · OPEN SOURCE`
- Headline, two lines, Fraunces: `Every skill is free` / `to download.`
- Terminal panel lines, monospace:
  - `$ omo run skills`
  - `→ download free · pay per run`
  - The `→ download free · pay per run` line is the ONE orange #FF6B3D
    highlight; everything else in the terminal is pine or deep mint #78CCA1.
- URL line under the terminal, DM Sans 700 pine: `github.com/omo-space/skills`
- CTA: `omo.space`

Reading order: eyebrow, headline, terminal panel, URL. Total words <= 18.

Safe area: keep all text inside the central 84% of the canvas; the pine frame
stays clear of text.

Negative constraints: no device mockup, no laptop mockup, no phone, no photo,
no 3D render, no gradients, no watermark, no fake UI, no other logos, no
dark hacker clichés, no black background, no glitch, no matrix green, no
neon, no extra words, no misspelled text, no second orange element, no black
or gray ink, no tilted elements. One finished social graphic, no device
mockup, no photo, no 3D render, no watermark, no fake UI, no extra words, no
misspelled text.
