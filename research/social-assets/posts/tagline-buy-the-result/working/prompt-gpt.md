# gpt-image-2 prompt — tagline-buy-the-result (issue #87)

One finished 16:9 landscape flat-vector social graphic, 1600x900 canvas.

Viewer takeaway: Omo sells the result, not another subscription — one honest,
warm, kitchen-table announcement.

Layout geometry — Omo master style, launch variant:
- Cream canvas #F8F7F5 filling ~50% of pixels as open breathing room.
- A single rounded-rect pine #17352C frame, 9px stroke, radius 28, inset 24px
  from the canvas edge. No double frame, no shadow, no white frame.
- Eyebrow top-left, uppercase DM Sans 700 letterspaced 20px, muted #5F6F68.
- Fraunces 600 headline bottom-left block, 3 lines, 72-88px, pine #17352C on
  cream (never on a field). Headline occupies columns 1-8.
- Hero object bottom-right (columns 9-12): the peach bean mascot.
- One small CTA pill bottom-left: cream #FFFFFF pill, pine DM Sans 700 text.

Theme: omo-sweet-pastel — warm flat-vector storytelling. Two pastel fields
only: one mint #BDEFD4 rounded organic blob and one peach #FFB89D blob,
behind the hero object, never behind text. Exactly one orange #FF6B3D accent
in the whole image (see negative constraints). Smooth pine outlines with round
caps, soft pine-tinted shadow only. No gradients, no gloss, no 3D, no photo,
no texture.

Illustration subject: the Omo peach bean — one organic peach #FFB89D
super-ellipse blob (bean shape, NOT a circle), tilted exactly -7 degrees about
its own center, with a tiny pine `•ᴗ•` face in bold pine. The bean sits on the
frame's top-left corner peeking over the stroke. Nothing else in the image
rotates.

Exact on-image strings (spelling must be exact):
- Eyebrow: `OMO · THE RESULT MARKETPLACE`
- Headline, three lines: `Buy the result,` / `not another` / `subscription.`
  with a single orange #FF6B3D strike-through line crossing out
  `subscription.` — the ONLY orange element in the image.
- Supporting line under headline: `Workflows, not rentals.`
- CTA pill: `omo.space`

Reading order: eyebrow, headline, supporting line, CTA. Total words <= 14.

Safe area: keep all text inside the central 84% of the canvas; the pine frame
stays clear of text.

Negative constraints: no device mockup, no phone, no photo, no 3D render, no
gradients, no watermark, no fake UI, no other logos, no extra words, no
misspelled text, no second orange element (only the one strike-through), no
black or gray ink, nothing else tilted. One finished social graphic, no device
mockup, no photo, no 3D render, no watermark, no fake UI, no extra words, no
misspelled text.
